%h1= @ingredient.name
- if @ingredient.image.present?
  = image_tag(@ingredient.image, width: '150', class: 'ingredient-image')

%ul
  - @ingredient.recipes.each do |recipe|
    %li= recipe.name


  
  %nav.nav-bottom
    = link_to 'Delete this ingredient', @ingredient, :method =>:delete, :confirm =>"Are you sure you want to delete this ingredient?"
    |
    = link_to 'Edit this ingredient', edit_ingredient_path(@ingredient)




